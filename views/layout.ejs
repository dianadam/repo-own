<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Penjadwalan Anak</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    /* Style khusus untuk tombol sholat */
    .prayer-btn {
      display: inline-block;
      margin: 4px;
      padding: 8px 12px;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      text-align: center;
      transition: 0.2s;
    }
    .prayer-btn.Belum {
      background-color: #f44336;
      color: #fff;
    }
    .prayer-btn.Sudah {
      background-color: #4caf50;
      color: #fff;
    }
    .prayer-btn:hover {
      opacity: 0.85;
    }
  </style>
</head>
<body>
  <!-- Navbar Responsive -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">📅 Penjadwalan Anak</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <!-- Selalu tampil -->
          <li class="nav-item"><a class="nav-link" href="/"><i class="bi bi-house-door"></i> Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/prayer"><i class="bi bi-moon-stars"></i> Praying</a></li>

          <% if (user) { %>
            <li class="nav-item"><a class="nav-link" href="/children"><i class="bi bi-people"></i> Daftar Anak</a></li>
            <li class="nav-item"><a class="nav-link" href="/new"><i class="bi bi-plus-circle"></i> Tambah Jadwal</a></li>
            <li class="nav-item"><a class="nav-link" href="/users"><i class="bi bi-person-gear"></i> Manajemen User</a></li>
            <li class="nav-item"><a class="nav-link" href="/logout"><i class="bi bi-box-arrow-right"></i> Logout (<%= user.username %>)</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login"><i class="bi bi-box-arrow-in-right"></i> Login</a></li>
          <% } %>
			<li class="nav-item"><a class="nav-link" href="/report"><i class="bi bi-moon-stars"></i> Report</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Konten -->
  <div class="container mt-4">
    <%- body %>
  </div>

  <!-- Bootstrap + Icons -->
 
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</body>
</html>
